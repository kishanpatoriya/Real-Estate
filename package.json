{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix backend\"",
    "start": "concurrently \"npm run start --prefix client\" \"npm run start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "ioredis": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "resend": "^6.22.1"
  }
}
